<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MenuFragment" >

    <Button
        android:id="@+id/buttonUploadImage"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:layout_marginTop="252dp"
        android:text="@string/upload_image"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.498"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/buttonUploadVideo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:text="@string/upload_video"
        app:layout_constraintEnd_toEndOf="@+id/buttonUploadImage"
        app:layout_constraintStart_toStartOf="@+id/buttonUploadImage"
        app:layout_constraintTop_toBottomOf="@+id/buttonUploadImage" />

    <Button
        android:id="@+id/buttonLiveFeed"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:text="@string/live_feed"
        app:layout_constraintEnd_toEndOf="@+id/buttonUploadVideo"
        app:layout_constraintStart_toStartOf="@+id/buttonUploadVideo"
        app:layout_constraintTop_toBottomOf="@+id/buttonUploadVideo" />
</androidx.constraintlayout.widget.ConstraintLayout>